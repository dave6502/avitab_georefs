# Python dependencies for download_airac project
# Generated based on analysis of all Python files in src/ and scripts/

# Core dependencies for AIRAC chart processing
requests>=2.28.0
pymupdf>=1.23.0
beautifulsoup4>=4.12.0
Pillow>=10.0.0

# Note: tkinter is part of Python standard library
# Note: xml.dom.minidom is part of Python standard library
# Note: argparse is part of Python standard library
# Note: pathlib is part of Python standard library (Python 3.4+)
# Note: re, os, sys, shutil, hashlib, csv, datetime are all standard library modules